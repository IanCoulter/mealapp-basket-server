
{
  "name": "mealapp-basket-server",
  "version": "1.0.0",
  "main": "server.js",
  "scripts": {
    "postinstall": "npx playwright install chromium --with-deps",
    "start": "node server.js"
  },
  "dependencies": {
    "express": "^4.18.2",
    "playwright": "^1.40.0"
  }
}
